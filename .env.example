# Répertoire de données SQLite (dans le conteneur : /app/data, monté en volume)
DATA_DIR=/app/data

# Provider flights-service (microservice Python auto-hébergé, fast-flights).
# Sans cette URL, FlyDeal utilise le moteur de simulation déterministe.
# FAST_FLIGHTS_URL=http://localhost:8000
