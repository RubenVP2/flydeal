# Répertoire de données SQLite (dans le conteneur : /app/data, monté en volume)
DATA_DIR=/app/data

# Provider Amadeus (optionnel). Sans ces clés, FlyDeal utilise le moteur de simulation déterministe.
# AMADEUS_CLIENT_ID=
# AMADEUS_CLIENT_SECRET=
