# Répertoire de données SQLite (dans le conteneur : /app/data, monté en volume)
DATA_DIR=/app/data

# Provider FlightAPI.io (optionnel). Sans cette clé, FlyDeal utilise le moteur de simulation déterministe.
# Clé obtenue sur https://www.flightapi.io/ (dashboard → API key).
# FLY_API_KEY=
